# secrets & env
.env
.env.local
.env.production
# Local Surveyor deployment copy. The committed .example file is safe.
backend/.env.surveyor
.venv/
venv/
# python
__pycache__/
*.pyc
*.pyo
# logs
*.log
# os
.DS_Store
# build
target/
node_modules/
*.bak
*.swp
*.swo

# local backups — contain copies of /etc/cordia/cordia.env and retired
# assets. Never commit: cordia.env holds the Postgres DSN and app passwords.
backups/

# local GGUF weights (~3.5GB) — never commit binaries this size
models/
